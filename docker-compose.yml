version: '3'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: cicd_jenkins_web:latest
    container_name: fastapi-app
    volumes:
      - ./app:/app
    ports:
      - "80:80"